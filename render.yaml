services:
  - type: web
    name: tds-virtual-ta
    env: python
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    workingDir: api
    plan: free
